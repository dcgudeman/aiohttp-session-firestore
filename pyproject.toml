[tool.poetry]
name = "aiohttp-session-firestore"
version = "0.1.0"
description = ""
authors = ["David Gudeman <dcgudeman@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
